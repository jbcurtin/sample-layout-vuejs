import AxiosClient from './axios-client';

// export default {
//     [REMOVE_TAGS]: ({commit, state}, tags) => {
//       return new Promise((resolve, reject) => {
//         AxiosClient.post(`/tag-video-remove/${state.videoHash}.json`, {tags: tags})
//         resolve({tags})
//       })
//     }
// }
